import React from 'react';
// import MealCard from '../MealCard';
// import { DEFAULT_MEALS_IN_A_DAY } from '../../constants';
// import MOCKED_MEALPLAN from '../../mockData';

const MealPlanDesktop = () =>
//   const render = () => Object.keys(DEFAULT_MEALS_IN_A_DAY).map((defaultMeal) => Object.keys(MOCKED_MEALPLAN).map((day) => (
//     <div key={defaultMeal} className="mealplan-default-meal-container">
//       <h2 className="mealplan-default-meal-title">{DEFAULT_MEALS_IN_A_DAY[defaultMeal]}</h2>
//       {MOCKED_MEALPLAN[day][defaultMeal].map(({ meal }) => (
//         <MealCard key={meal.id} meal={meal} />
//       ))}
//     </div>
//   )));
  // eslint-disable-next-line implicit-arrow-linebreak
  (
  // <>
    <div className="meal-plan-meals-container">
        TEST
      {/* render() */}
    </div>
  // </>
  );
export default MealPlanDesktop;
